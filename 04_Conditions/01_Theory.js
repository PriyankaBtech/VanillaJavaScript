/* conditional statements execute code based on a particular condition.

JavaScript has two types of conditional Statements:

1. If Else Statement - It executes the if block if the condition is true and the else block if it is false.

If Statement
If Else Statement
Else if Statement
Nested if Statement
Nested if else Statement


2. Switch Statement - It executes one of the cases among multiple alternatives. */
